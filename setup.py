from setuptools import setup, find_packages

if __name__ == '__main__':

    setup(
        name = 'linkedin_jobs_scrapper',
        version = '1.0',
        author = 'Pedro Sousa Silva',
        packages = find_packages()
    )